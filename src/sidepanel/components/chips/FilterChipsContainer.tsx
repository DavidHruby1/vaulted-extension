import styles from './FilterChipsContainer.module.css';

export const FilterChipsContainer = () => {
    return (
        <div className={ styles['chips-container'] }>
            {/* There will be FilterChip items */}
        </div>
    );
}